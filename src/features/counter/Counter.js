import { useSelector, useDispatch } from 'react-redux';
import {
  selectCount,
} from './counterSlice';

export default function Counter() {

  return (
    <div>
      <div>
      
      </div>
    </div>
  );
}
